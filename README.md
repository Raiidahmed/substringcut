# substringcut

A simple Python utility that splits long strings into chunks of a maximum length (default 2048 characters) for feeding into the OpenAI API context window.

## Files

- `main.py` — `split_string()` function and example usage with a meeting transcript
